$PBExportHeader$uf_bitor.srf
$PBExportComments$By PBKiller v2.5.18(http://kivens.nease.net)
global type uf_bitor from function_object
end type

forward prototypes
global function ulong uf_bitor (ulong al_val1,ulong al_val2)
end prototypes

global function ulong uf_bitor (ulong al_val1,ulong al_val2);ulong ll_ret
integer li_i

for li_i = 0 to 31

	if mod(long(al_val1 / 2 ^ li_i),2) + mod(long(al_val2 / 2 ^ li_i),2) > 0 then
		ll_ret += 2 ^ li_i
	end if

next

return ll_ret
end function

