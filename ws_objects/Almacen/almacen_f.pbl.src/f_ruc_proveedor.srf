$PBExportHeader$f_ruc_proveedor.srf
global type f_ruc_proveedor from function_object
end type

forward prototypes
global function string f_ruc_proveedor (string as_proveedor)
end prototypes

global function string f_ruc_proveedor (string as_proveedor);String ls_ruc

Select ruc into :ls_ruc from proveedor
  where proveedor = :as_proveedor;

if isnull( ls_ruc) then ls_ruc = ''
  
Return ls_ruc
end function

