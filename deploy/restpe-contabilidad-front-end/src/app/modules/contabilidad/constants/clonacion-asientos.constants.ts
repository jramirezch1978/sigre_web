/**
 * Mensajes de error para el proceso de clonación de asientos.
 */
export const CLONACION_ASIENTOS_MENSAJES_FALLBACK = {
  ERROR_CARGA: 'No se pudieron cargar los asientos disponibles para clonación.',
} as const;
