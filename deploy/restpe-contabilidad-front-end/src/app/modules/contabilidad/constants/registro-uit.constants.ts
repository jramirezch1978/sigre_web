/**
 * REGISTRO_UIT_MENSAJES_FALLBACK — Mensajes por defecto cuando la API
 * no retorna una descripción explícita.
 */
export const REGISTRO_UIT_MENSAJES_FALLBACK = {
  GUARDADO_DESCRIPCION:    '¡UIT registrada exitosamente!',
  ACTUALIZADO_DESCRIPCION: '¡UIT actualizada exitosamente!',
} as const;
