/**
 * Mensajes de error para la selección de cuentas contables.
 */
export const SELECCIONAR_CUENTA_CONTABLE_MENSAJES_FALLBACK = {
  ERROR_CARGA: 'No se pudieron cargar las cuentas contables.',
} as const;
