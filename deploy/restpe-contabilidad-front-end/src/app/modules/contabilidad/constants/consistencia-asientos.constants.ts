/**
 * Mensajes de error para el proceso de consistencia de asientos.
 */
export const CONSISTENCIA_ASIENTOS_MENSAJES_FALLBACK = {
  ERROR_CARGA: 'No se pudieron cargar los registros de consistencia de asientos.',
} as const;
