/**
 * Mensajes de fallback para el Reporte de Validación Contable.
 */
export const REPORTE_VALIDACION_MENSAJES_FALLBACK = {
  ERROR_CARGA: 'No se pudo cargar el reporte de validación. Intente nuevamente.',
};
