/**
 * MAESTRO_CONTABLE_MENSAJES_FALLBACK — Mensajes por defecto
 * para la retroalimentación de UI del Maestro Contable.
 */
export const MAESTRO_CONTABLE_MENSAJES_FALLBACK = {
  ERROR_CARGA: 'Error al cargar el maestro contable. Intente nuevamente.',
} as const;
