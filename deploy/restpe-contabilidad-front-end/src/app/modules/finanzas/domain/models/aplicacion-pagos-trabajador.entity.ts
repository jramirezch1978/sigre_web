export interface AplicacionPagosTrabajadorEntity {
  apt_codigo: string;
  apt_nombreTrabajador: string;
  apt_centroCosto: string;
  apt_tipoDoc: string;
  apt_numDocumento: string;
  apt_entidadBancaria: string;
  apt_numCuenta: string;
  apt_netoAPagar: number;
}
