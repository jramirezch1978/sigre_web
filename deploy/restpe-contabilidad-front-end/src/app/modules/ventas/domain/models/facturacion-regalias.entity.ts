export interface FacturacionRegaliasEntity {
  factura_codigo: string;
  factura_franquicia: string;
  factura_ruc: string;
  factura_periodo: string;
  factura_base_calculo: number;
  factura_porcentaje_regalias: number;
  factura_monto_base: number;
  factura_monto_regalia: string;
  factura_impuesto: string;
  factura_monto_impuesto: string;
  factura_moneda: string;
  factura_monto_factura: string;
  factura_observaciones: string;
  factura_estado: string;
}
