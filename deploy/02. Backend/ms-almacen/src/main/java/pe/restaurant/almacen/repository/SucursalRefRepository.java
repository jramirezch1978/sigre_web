package pe.restaurant.almacen.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import pe.restaurant.almacen.entity.SucursalRef;

public interface SucursalRefRepository extends JpaRepository<SucursalRef, Long> {
}
