package pe.restaurant.almacen.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import pe.restaurant.almacen.entity.UnidadMedidaRef;

public interface UnidadMedidaRefRepository extends JpaRepository<UnidadMedidaRef, Long> {
}
