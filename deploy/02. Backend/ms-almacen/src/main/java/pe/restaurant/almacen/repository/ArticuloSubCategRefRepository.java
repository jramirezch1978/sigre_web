package pe.restaurant.almacen.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import pe.restaurant.almacen.entity.ArticuloSubCategRef;

public interface ArticuloSubCategRefRepository extends JpaRepository<ArticuloSubCategRef, Long> {
}
