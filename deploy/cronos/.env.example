# SIGRE ERP — Variables de entorno (copiar a .env en cronos)
# cp .env.example .env && nano .env

POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_POSTGRES
ERP_APP_PASSWORD=CHANGE_ME_ERP_APP
SONAR_DB_PASSWORD=CHANGE_ME_SONARQUBE

IMAGE_REGISTRY=sigre
IMAGE_TAG=latest

PUBLIC_HOST=crisaor.serveftp.com
API_GATEWAY_URL=http://crisaor.serveftp.com:9080
FRONTEND_URL=http://crisaor.serveftp.com:8080

TZ=America/Lima

# Clave AES-256 (32 bytes en base64). Generar: python -c "import base64,secrets; print(base64.b64encode(secrets.token_bytes(32)).decode())"
APP_ENCRYPTION_KEY=U2lncmVFcnBXZWJBZXMyNTZLZXlEZXYyMDI2ISEhISE=
JWT_SECRET=dGhpcyBpcyBhIHNlY3JldCBrZXkgZm9yIHNpZ3JlLXdlYiBlcnAgand0IHRva2VuIHNpZ25pbmc=
APP_PROVISION_SECRET=dev-provision-cambiar-produccion

# RabbitMQ (stack infra — hostname interno: rabbitmq)
RABBITMQ_PASSWORD=sigre

# BD asistencia-service (contenedor db-sigre-web — puerto NAT 5433)
ASISTENCIA_DB_PASSWORD=S1greW3b@2025!
ASISTENCIA_DB_HOST=crisaor.serveftp.com
ASISTENCIA_DB_PORT=5433

# JDBC interno (red Docker stack_default — hostname: postgres)
# jdbc:postgresql://postgres:5432/sigre_security
# jdbc:postgresql://postgres:5432/sigre_template
# jdbc:postgresql://postgres:5432/sigre_emp_cantabria
