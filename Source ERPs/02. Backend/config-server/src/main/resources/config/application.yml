# Configuración central servida a clientes (modo nativo).
# Convención Spring Cloud Config: por servicio y perfil, por ejemplo:
#   ms-almacen-dev.yml
#   api-gateway-prod.yml
# Los valores aquí se fusionan con lo que los clientes pidan por /{application}/{profile}.
info:
  restaurant:
    config:
      server: native
      note: "Agregar archivos YAML en este directorio según necesidad."
