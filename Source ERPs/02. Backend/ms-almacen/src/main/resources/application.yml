server:
  port: 9003
spring:
  application:
    name: ms-almacen
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        default_schema: almacen
        dialect: org.hibernate.dialect.PostgreSQLDialect
        boot:
          allow_jdbc_metadata_access: false
  flyway:
    enabled: false
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  health:
    db:
      enabled: false
app:
  almacen:
    integracion:
      validar-tres-vias: ${ALMACEN_VALIDAR_TRES_VIAS:true}
      tolerancia-tres-vias: ${ALMACEN_TOLERANCIA_TRES_VIAS:0.0001}
  testdata:
    enabled: ${APP_TESTDATA_ENABLED:true}
  security-datasource:
    jdbc-url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_SECURITY_NAME:restaurant_pe_security}?sslmode=${DB_SSL_MODE:disable}
    username: ${DB_USER:restaurant_admin}
    password: "${DB_PASS:Erp_restpe#2026}"
  tenant:
    jdbc-ssl-mode: ${DB_SSL_MODE:disable}
    pool:
      maximum-pool-size: ${TENANT_POOL_MAX:3}
      minimum-idle: ${TENANT_POOL_MIN_IDLE:0}
      idle-timeout-ms: ${TENANT_POOL_IDLE_TIMEOUT:30000}
      max-lifetime-ms: ${TENANT_POOL_MAX_LIFETIME:120000}
  jwt:
    secret: ${JWT_SECRET:dGhpcyBpcyBhIHNlY3JldCBrZXkgZm9yIHJlc3RhdXJhbnQucGUgZXJwIGp3dCB0b2tlbiBzaWduaW5n}
    access-token-expiration: ${JWT_ACCESS_MS:900000}
    definitive-token-expiration: ${JWT_DEFINITIVE_MS:900000}
    refresh-token-expiration: ${JWT_REFRESH_MS:604800000}
  encryption:
    key: ${APP_ENCRYPTION_KEY:UmVzdGF1cmFudFBlQWVzMjU2S2V5RGV2MjAyNiEhISE=}
api:
  gateway:
    url: ${API_GATEWAY_URL:http://localhost:8080}
client:
  ms-contabilidad:
    url: ${CLIENT_MS_CONTABILIDAD_URL:${api.gateway.url}}
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 10000
        loggerLevel: basic
  compression:
    request:
      enabled: true
    response:
      enabled: true
springdoc:
  api-docs:
    path: /api/almacen/v3/api-docs
  swagger-ui:
    path: /api/almacen/swagger-ui.html
