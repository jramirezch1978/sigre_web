se solicita que uno de los primeros trabajos que va a hacer el ms-worker, es la sincronizacion de las tablas y datos de las tablas maestras de la base de datos template a cada uno de las base de datos tenant, para ello se dividiría en tres fases

Paso inicial = de la base de datos template, toma el listado de las tablas, y luego consulta todas las empresas, y por cada registro se conecta al tenant, y extrae el listado de tablas de ese tenant y luego 

fase 1.- si la tabla no existe en el tenant, se crea con la estrutura de la tabla del template, con sus indices, sus FK, sus contraints, sus secuencias, sus unique key, y a data que lo contiene

Fase 2.- si la tabla existe, entonces obtiene las columnas y los tipos de datos de cada columna, y lo compara con las columnas de la tabla del tenant, si el campo no existe, entonces se agrega junto con el FK de este campo, en caso lo tuviera, si el campo existe, se debe actualizar el tipo de datos con la longitud y escala, siempre y cuando que el tamaño de columna sea menor al del tenant, osea por ejemplo si el tenant tiene un varchar (100) y el template tiene un varcahr(50) no haces el cambio, porque el tamaño del tennat es mayor que el template, pero si el tenant tiene un varchar (50) y el template tiene un varcahr(100) entonces se actualiza el tamaño de la columna del tenant a 100, y se actualiza el tipo de dato en caso difiera

Fase 3.- debes de obtener la estructura de los indices, de la tabla template (origen) y lo busca en la tabla del tenant (detino), si existe algun indice que cumpla con la estructura entonces no hagas ningun cambio, si no existe ningun indice de la tabla destino que cumpla con la estructura de las columnas que conforman el indice (o la formaula) entonces se crea el indice en la tabla del tenant, teniendo en cuenta la estructura IX_TABLENAME_XX, donde la XX es un autonumerico incremental, osea tomas el ultino indice, obttienes el sufijo XX y le agrega 1 para el nuevo indice, ojo con ello; igual con los FK y con los UX y los contraints, todo eso se evalua en este paso

ya sea en la FASE 1 o FASE 2 o FASE 3, si es que estas intentando crear o dar un FK a otra tabla, adebes de asegurarte que la tabla destino tenga la llave primaria que concuerde con los campos de la llave foranea, en tal caso debes de crear la llave primaria primero, y si este cambio sucedio en la fase 1 o fase 2, entonces lo marcas para que no sea ejecutado en la fase 3 (me refiero a la creación de la llave primaria en la tabla destino)

BAJO NINGUNA CONDICION O MOTIVO SE PERMITE ELIMINAR TABLAS QUE EXISTAN EN LOS TENANTS Y QUE NO ESTE EN TEMPLATE, IGUALMENTE CON LAS COLUMNAS, INDICES, FK, SECUENCIAS, ETC, SOLO ESTA PERMITIVO AGREGAR Y MODIFICAR, NO SE PERMITE ELIMINAR NADA. no debe generar ni warning, ya que los tenant pueden tener sus propias personalizciones, la idea es que los cambios y adiciones de template se propaguen a los tenants; y no al reves, los tenant pueden tener nuevos indices, FK, UK, tablas, columnas que el template no tiene, y no se deben de eliminar, tampoco se debe prograpar dichos cambios de los tenant a template, solo se permite de template a los tenant y no al reves.

tener cuidado con la fase 3, ya que la fase 1 crea la tabla con sus indices, FK, UX y todo lo demas, estos indices, fk, ux, constraints no deberían ejecutarse en la fase 3, ya que ya lo hizo la fase 1, solo debería ser para las tablas que ya existen (que estan en template y que tambien estan en el tenant), no para los que no existen en el tenant. TEnlo presente para no estar haciendo ejecuciones innecesarias.

En la fase 1, cuando se crea la tabla que no existe en el tenant, obviament se debe guardar todo lo que ha se ha creado (la tabla), pero tener cuidado que si la columna teiene un FK a otra tabla, debes de validar previamente que dicho campo existe en dicha tabla, si no existe, lo creas primero y luego haces la FK, y lo marcas (de alguna manera, posiblemente guardando eso en el log con el id de ejecución) para que la fase 2 ya no lo cree neuavemtene, porque ambos son ejecuciones paralelas, me dejo entender; todo esto debe realzar en la fase 1, no en la fase 2 ni 3.

el ms.worker debe leer la cantidad de minutos de espera o de disparo (en minutos) del config-server, revisa el config-server y analiza la estructura del mismo, y la idea es que al momento de arrancar el ms-worker se programe este evento en los n minutos que indica el config-server, el evento se ejecuta, realiza la sincronizacion, y una vez terminado, vuelve a progrmarse para ejecutarse dentro de n minutos, nuevamente pasan esos n minutos, se dispara el evento, se realiza la sincronizacion, y vuelve a programarse, y asi sucesivamente.

debes de usar la librería common, para evitar repetir codigo.

Necesito que la fase 1 y la fase 2 se hagan en forma paralela, de manera asyncrona, usando hilos, mientras que la fase 3, recien comenzaría cuando termine la fase (por cada registro de empresa).

Ojo la variable de N minutos debe ed leerlo del config-server, para ello revisa la estructura del config-server, verifica en que rama se estan haciendo los cambios de los parametros para config-server.

procura guardar en el log datos necesarioas para hacerle seguimiento y auditoría al trabajo de sincronización, incluyendo el tiempo total que ha tomado realizarlo

Otro punto importante, todo el proceso de sincronización debe ser auditado, para ello se debe guardar en la tabla auditoria.schema_sync_cron_log, el registro de la ejecución, y los detalles de cada objeto que se ha sincronizado, y el tiempo que ha tomado cada fase, y el tiempo total que ha tomado el proceso.

aca esta la estructura de la tabla auditoria.schema_sync_cron_log:
CREATE TABLE IF NOT EXISTS auditoria.schema_sync_cron_log (
    id                  BIGSERIAL       NOT NULL,
    execution_id        VARCHAR(36),
    status              VARCHAR(20)     NOT NULL,   --INICIO, PROCESANDO, TERMINADO, FALLIDO (Si es fallido, se debe guardar el error en el campo error_detail)
    dry_run             BOOLEAN         NOT NULL DEFAULT FALSE,
    tenant_db_name      VARCHAR(100),
    message_final       TEXT,
    is_failed           BOOLEAN         NOT NULL DEFAULT FALSE,
    is_changed          BOOLEAN         NOT NULL DEFAULT FALSE,
    nombre_empresa      VARCHAR(100),
    statements_execute  text,
    schema_object       VARCHAR(100),
    objects_summary     TEXT,
    object_type         VARCHAR(20),    --TABLA, CAMPO, INDICE, SECUENCIA, FK, etc.
    duration_ms         BIGINT          NOT NULL,
    error_detail        TEXT,
    created_at          TIMESTAMPTZ     NOT NULL DEFAULT NOW(),

    CONSTRAINT pk_schema_sync_cron_log PRIMARY KEY (id)
);

puedes entender cada columna que compone la tabla, y su significado?.

LAs tres fases de sincronizacion lo debes aplicar empresa por empresa, osea obtienes el primer registro de la ermpesa, y aplica las tres fases de sincronizacion, luego pasa al siguiente registro y aplicas las tres fases, y asi sucesivamente, no pasas al siguiente registro hasta no terminar las tres fases de sincronizacion del registro anterior.

ms-worker esta funcionando muy bien , pero ahora hay que agregar un 4 fase que es llenar los registros de algunas tablas maestras, las cuales estarían en una propiedad de config-server, los nombres de las tablas estarían separados por comas, y serían estas por ahora, conforme avance se iran agergando mas tablas: 

articulo_categ, 
articulo_clase, 
articulo_sub_categ, 
articulo_mov_tipo, 
impuestos_tipo, 
almacen_tipo (este es una tabla nueva en template, debe tener los tipos de almacenes => materia prima, producto terminado, subproducto, insumo, activo fijo, por favor lo añades al script de carga inicial de maestros en template)
Calendario,
Calendario_feriado,
Pais,
Conversion_unidad,
Forma_pago,
Motivo_nota,
cencos_niv1
cencos_niv2
cencos_niv3,
centros_costo,
color
doc_tipo
motivo_nota
proveedor_tipo
unidad
unidad_conv
admin_afp

busca las tablas equivalentes en ddl, y si no se llenan por dejfecto, toma los datos que estan en la carpeta 03. base de datos/data y que se llenen desde el template para que al ejecutar el worker llenen a los tenant

ten cuidado que si en los tenan el registro ya existe, solo actualizalo, para ello debes de buscar por el campo codigo, no por el id; y lo incluyes en el email que envias al final de la sincronizacion.

repito no crees nuevas tablas, busca la tabla mas equivalente en los ddl del template.
