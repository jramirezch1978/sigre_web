spring:
  application:
    name: sync-service
  cloud:
    config:
      uri: http://10.100.14.102:8888
      fail-fast: true
      retry:
        initial-interval: 3000
        max-attempts: 5

---
# Perfil para Docker
spring:
  config:
    activate:
      on-profile: docker
  cloud:
    config:
      uri: http://config-server:8888
