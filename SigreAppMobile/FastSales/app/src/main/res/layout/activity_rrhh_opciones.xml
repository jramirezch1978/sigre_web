<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5">

    <!-- Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorPrimary"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:titleTextColor="@android:color/white"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"/>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp"
            android:gravity="center">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="👥 RECURSOS HUMANOS"
                android:textSize="24sp"
                android:textStyle="bold"
                android:textColor="@color/colorPrimary"
                android:layout_marginTop="32dp"
                android:layout_marginBottom="32dp"/>

            <!-- Botón Actualizar Fotos -->
            <Button
                android:id="@+id/btnActualizarFotos"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="📷 ACTUALIZAR FOTOS PERSONAL"
                android:textColor="#FFFFFF"
                android:textSize="16sp"
                android:padding="16dp"
                android:background="@drawable/login_button_primary"
                android:layout_marginBottom="16dp"
                android:drawableStart="@android:drawable/ic_menu_camera"
                android:drawablePadding="12dp"/>

            <Button
                android:id="@+id/btnParteDestajo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="📋 PARTE DE DESTAJO"
                android:textColor="#FFFFFF"
                android:textSize="16sp"
                android:padding="16dp"
                android:background="@drawable/login_button_primary"
                android:layout_marginBottom="16dp"/>

            <Button
                android:id="@+id/btnJornalCampo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="👷 JORNALES DE CAMPO"
                android:textColor="#FFFFFF"
                android:textSize="16sp"
                android:padding="16dp"
                android:background="@drawable/login_button_primary"
                android:layout_marginBottom="16dp"/>
        </LinearLayout>
    </ScrollView>

    <!-- Botón Salir -->
    <Button
        android:id="@+id/btnSalir"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="← VOLVER AL MENÚ PRINCIPAL"
        android:textColor="#FFFFFF"
        android:background="@drawable/login_button_config"
        android:padding="16dp"
        android:layout_margin="16dp"/>
</LinearLayout>
